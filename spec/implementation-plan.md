# 实施计划

## 1. 计划用途

本文是可逐项执行的开发清单，不只是产品里程碑。每个阶段必须满足自己的验收命令和退出条件后，才能进入下一阶段。除明确标记为“在线阶段”的任务外，首版必须在不启动后端服务的情况下完成人机对局。

状态约定：

- `[ ]` 尚未开始。
- `[~]` 正在进行。
- `[x]` 已完成并通过本阶段验收。
- `[!]` 被明确问题阻塞，必须在条目后记录原因。

## 2. 当前仓库基线

截至 2026-07-28，仓库实际状态如下：

- 根目录是单包 Vite + React + TypeScript 应用。
- 入口为 `src/main.tsx` 和 `src/App.tsx`，棋盘原型使用 DOM/CSS 绘制。
- 当前脚本为 `npm run dev`、`npm run build`、`npm run test` 和 `npm run preview`。
- 当前只有 Vitest/Testing Library 测试，没有端到端测试。
- 尚未安装 PixiJS、XState、tween.js 或 React Router。
- 尚未建立 `apps/`、`packages/` 或独立游戏服务器。
- `example_screenshots/` 已被 Git 忽略，仅作为本地视觉和玩法参考。

重构原则：保留 Vite 构建方式，把现有单包应用原样迁入 `apps/web`，先保证行为、测试和构建不变，再提取共享规则包。旧 DOM 棋盘暂时作为 `/play` 的实现，直到 PixiJS 核心样片接管；不维护第二套 Web 框架或重复应用。

## 3. 依赖关系与关键路径

```text
阶段 0 基线保护
  └─ 阶段 1 Vite + workspace
       └─ 阶段 2 共享包与协议
            └─ 阶段 3 规则内核
                 └─ 阶段 4 离线人机
                      └─ 阶段 5 PixiJS 核心样片
                           └─ 阶段 6 完整 65 格对局
                                └─ 阶段 7 桌面版发布质量
                                     └─ 阶段 8 在线对局
```

阶段 2 至阶段 4 可以先使用无画面的测试驱动开发，但阶段 5 不得绕过已经确定的命令和 authority 接口。阶段 8 不得提前把离线人机模式改成必须连接服务器。

## 4. 阶段 0：基线保护

目标：确保工程迁移前有可比较、可回退的行为基线。

当前状态：`[x]` 已完成。三个桌面尺寸截图因浏览器运行时没有可用实例，经用户确认跳过；其余基线证据见 [baseline-2026-07-28.md](./baseline-2026-07-28.md)。

### 任务

- [x] 记录当前 Node.js 和 npm 版本，并在根目录声明项目支持的 Node.js LTS 范围。
- [x] 运行现有 `npm run test` 和 `npm run build`，记录已存在的失败，不能把旧失败归因于迁移。
- [x] 为当前原型保存桌面截图：`1280 x 720`、`1600 x 900`、`1920 x 1080`。经确认跳过（2026-07-28 用户确认）；原阻塞原因为浏览器运行时返回 `No browser is available` 且浏览器列表为空。
- [x] 记录当前可复用的数据：事件、道具、格子坐标、地标名称和 CSS 色值。
- [x] 确认 `example_screenshots/`、临时抽帧和本地解码工具继续保持未跟踪状态。
- [x] 给迁移前状态建立 Git 提交；不创建长期维护的 legacy 分支。

### 验收

```powershell
npm run test
npm run build
git status --short
```

退出条件：现有应用的测试、构建和视觉基线都有记录，工作区没有混入参考录像或临时文件。

## 5. 阶段 1：Vite workspace 重构

目标：不更换 Web 框架，把现有 Vite 应用迁入正式 monorepo 目录并保持行为基线。

当前状态：`[x]` 已完成。Vite 应用已迁入 `apps/web`，根脚本、客户端路由、静态检查、测试与构建均通过。浏览器运行时仍无可用实例，因此本阶段以三条路由的 HTTP 200 响应和 jsdom 路由测试完成非视觉验收；视觉回归仍按阶段 5 的 Playwright 任务执行。

### 目录与脚本

- [x] 根 `package.json` 改为 npm workspaces，范围为 `apps/*` 和 `packages/*`。
- [x] 创建 `apps/web` package，将现有 `index.html`、Vite 配置、`src/` 和 Web 测试迁入其中。
- [x] 将依赖固定为锁文件中的明确版本，不继续使用 `latest` 作为正式依赖范围。
- [x] 根目录提供统一脚本：`dev`、`build`、`test`、`typecheck`、`lint`。
- [x] 配置 ESLint 的 React、Hooks 与 TypeScript 规则，使根 `npm run lint` 可以在 CI 中无交互执行。
- [x] 首阶段的 `npm run dev` 只需启动 `apps/web`；游戏服务器尚不存在也不能报错。
- [x] 将共享 TypeScript 基础配置放入根目录或专用 config package。
- [x] 初期直接使用 npm workspaces 脚本，不为少量包额外引入任务编排框架。

### Web 外壳

- [x] 安装 React Router，创建 `/`、`/play` 和 `/room/:roomCode` 三个客户端路由。
- [x] `/` 第一屏直接显示模式准备，不制作营销落地页。
- [x] `/play` 暂时承载现有 DOM 原型，阶段 5 再替换为 PixiJS `GameShell`。
- [x] `/room/:roomCode` 首阶段只显示未开放状态，不连接服务器。
- [x] 路由切换后页面状态和测试稳定，直接刷新深层路由时由开发服务器返回 SPA 入口。
- [x] 保留现有全局字体、颜色和页面尺寸基线，暂不重做旧棋盘视觉。

### 旧应用处理

- [x] 移动后确认 `src/game/data.ts`、类型和现有三项测试仍工作，不在此阶段改写规则。
- [x] 删除根目录中已经迁入 `apps/web` 的重复入口和配置。
- [x] 不创建 `/legacy` 页面；Git 历史负责保存迁移前结构。

### 验收

```powershell
npm install
npm run dev
npm run typecheck
npm run test
npm run build
```

退出条件：根命令可以启动并构建 `apps/web` Vite SPA；`/`、`/play`、`/room/test` 可访问；现有三个测试继续通过；根目录不再保留重复 Web 入口。

## 6. 阶段 2：共享包与协议骨架

目标：先固定依赖方向和可序列化边界，再编写完整规则。

当前状态：`[x]` 已完成。四个共享包均可独立执行类型检查、测试和构建；协议 JSON 往返与结构化错误测试通过，静态边界检查确认不存在共享包指向应用层的依赖。现有 Web 原型已从 `game-content` 读取事件和道具，完整规则迁移仍按阶段 3 执行。

### 包结构

- [x] 创建 `packages/game-core`：纯类型、随机数、规则函数和 authority reducer。
- [x] 创建 `packages/game-content`：地图、事件、道具、皮肤与规则集配置。
- [x] 创建 `packages/game-ai`：电脑决策策略，只依赖允许公开的规则视图。
- [x] 创建 `packages/game-protocol`：命令、快照、领域事件、表现提示和运行时校验。
- [x] 每个包拥有独立入口、类型检查和测试脚本，不从 `apps/web` 反向导入。

### 依赖约束

- [x] `game-core` 不依赖 React、Vite、PixiJS、XState、DOM、Node.js API 或数据库。
- [x] `game-ai` 只依赖 `game-core` 的公开规则视图和命令类型。
- [x] `game-content` 只提供结构化定义，不包含 UI 组件或任意可执行脚本。
- [x] `game-protocol` 使用 Zod 校验 JSON 输入；协议类型从 schema 推导，避免手写两套结构。
- [x] `apps/web` 负责组合共享包，不能在 UI 中复制规则实现。当前 DOM 原型的旧规则流程仅作为迁移输入保留，不作为共享规则实现。

### 最小协议

- [x] 定义 `GameCommand`、`CommandEnvelope`、`GameSnapshot`、`DomainEvent` 和 `PresentationCue`。
- [x] 定义 `GameAuthorityPort.submit(command)` 与订阅快照/事件的接口。
- [x] 定义稳定的 `gameId`、`playerId`、`commandId`、`revision` 和版本字段。
- [x] 建立 JSON 往返测试，禁止函数、类实例、Map、Set、DOM 或 PixiJS 对象进入快照。
- [x] 对未知命令、未知内容 ID、重复命令和过期 revision 给出结构化错误。

### 验收

```powershell
npm run typecheck
npm run test
npm run build
```

退出条件：四个共享包可以独立构建；协议 JSON 往返保持等价；依赖检查不存在从共享包指向应用层的引用。

## 7. 阶段 3：确定性规则内核

目标：在没有 React 和 PixiJS 的情况下完成一局规则推进。

当前状态：`[x]` 已完成。纯规则内核、默认与测试地图、可恢复随机数、统一移动/效果管线和浏览器可用的 `LocalAuthority` 已实现；固定种子回放、快照恢复、幂等命令及阶段必测用例均通过。旧 DOM 原型仍仅作为迁移基线，正式规则写入只通过 reducer 与 authority。

### 地图与参与者

- [x] 定义 `MapDefinition`、`BoardSpace`、`LandmarkDefinition` 和地图注册表。
- [x] 导入默认地图的 0 至 65 格顺序，明确第 63、64、65 格为 `winningSpaceIds`。
- [x] 第 63、64、65 格同时属于喧声屋地标，但胜利集合与地标范围分开配置。
- [x] 创建一张不同格数的测试地图，验证规则没有硬编码 65。
- [x] 定义 2 至 4 个座位和 `local / ai / remote` 控制器类型，不增加角色属性字段。

### 随机、移动与结算

- [x] 实现带种子且可保存游标的随机数服务；业务代码禁止直接调用 `Math.random()`。
- [x] 实现两颗移动骰和两颗检定骰，并记录原始骰面。
- [x] 实现路径计算、终点折返、胜利判断、碰撞、暂停和额外行动纯函数。
- [x] 普通移动和事件移动复用同一落点处理管线。
- [x] 进入胜利状态后拒绝新的玩法命令。

### 内容效果

- [x] 建立可穷举 `GameEffect` 联合类型。
- [x] 实现移动、暂停、获得道具、交换位置、额外行动和临时骰子规则。
- [x] 检定只读取公开骰子结果；内容门槛必须位于骰组可达范围。
- [x] 建立事件三选一、道具持有/替换和最近事件降权逻辑。
- [x] 内容加载时验证 ID 唯一、引用存在、效果合法和资源清单格式。

### Authority 与回放

- [x] 实现浏览器可用的 `LocalAuthority`，它是离线对局唯一规则写入点。
- [x] 每个合法命令产生递增 revision、领域事件和必要表现提示。
- [x] 固定种子加固定命令序列必须得到相同快照和事件序列。
- [x] 从任意快照恢复后继续执行，结果必须与不中断对局一致。

### 必测用例

- [x] 第 62 格掷出跨越终点的路径和最终胜负符合玩法规格。
- [x] 普通移动或事件移动落入第 63、64、65 格都会立即结束对局。
- [x] 2、3、4 人的轮次递增准确；暂停和额外行动不破坏座次。
- [x] 同格多棋子的碰撞顺序确定且可回放。
- [x] 更换 `skinId` 不改变任何骰子、AI、事件或移动结果。

### 验收

```powershell
npm run typecheck
npm run test --workspace packages/game-core
npm run test --workspace packages/game-content
npm run build
```

退出条件：所有核心规则由纯函数或 authority reducer 决定；固定种子可复现；不同格数测试地图通过。

## 8. 阶段 4：离线人机闭环

目标：不用游戏服务器完成 `1v1`、`1v2`、`1v3` 的无画面完整对局和基础准备 UI。

当前状态：`[x]` 已完成。公开 `GameDecisionView`、可解释 AI、统一命令控制器和离线对局组合已接入；三个模式分别使用独立种子区间完成 1,000 局模拟，完成率均为 100%，没有死锁、非法命令、最大轮数超限或异常终止。详细结果见 [stage-4-simulation.md](./stage-4-simulation.md)。

### 模式与控制器

- [x] 实现 `OfflineMatchConfig`，模式只允许 `1v1`、`1v2`、`1v3`。
- [x] 准备界面使用三段选择器，默认选中 `1v1`。
- [x] 本地控制器将 UI 意图转换为 `GameCommand`。
- [x] AI 控制器根据合法命令集合、位置、碰撞、道具和事件收益提交同样的 `GameCommand`。
- [x] AI 随机偏差使用 authority 注入的种子随机源，不直接调用全局随机数。
- [x] AI 思考延迟只属于客户端表现，不进入规则快照；本阶段无画面控制器不引入延迟。

### AI 策略

- [x] 实现事件成功概率与成功/失败收益评分。
- [x] 实现主动道具使用和新旧道具替换决策。
- [x] 考虑终点折返、撞击其他参与者和当前名次。
- [x] AI 只读取显式公开的 `DecisionView`，不读取未来事件或隐藏随机状态。
- [x] 为决策输出原因标签，开发日志可以解释 AI 为什么选择该命令。

### 自动模拟

- [x] 提供无 UI 模拟入口，可指定模式、局数和种子范围。
- [x] 每个模式至少自动运行 1,000 局。
- [x] 统计完成率、平均轮数、胜率、事件频率和异常终止原因。
- [x] 将死循环、非法命令或超过最大轮次视为测试失败。

### 验收

```powershell
npm run test --workspace packages/game-ai
npm run simulate -- --mode 1v1 --games 1000
npm run simulate -- --mode 1v2 --games 1000
npm run simulate -- --mode 1v3 --games 1000
npm run build
```

退出条件：三个模式都能完成确定性人机对局；AI 不直接修改规则状态；模拟中不存在死局或非法状态。

## 9. 阶段 5：PixiJS 核心体验样片

目标：用最终架构和接近最终质量的表现完成约 15 格流程。

当前状态：`[x]` 已完成。独立 16 格样片已通过正式 `LocalAuthority`、PixiJS 场景、XState 表现状态机和 React UI 完成人机回合、事件选择与骰子检定闭环；三个桌面尺寸和连续 20 次场景重建均通过 Playwright。详细结果见 [stage-5-pixi-slice.md](./stage-5-pixi-slice.md)。

### 场景基础

- [x] 在 `apps/web` 安装并固定 PixiJS v8、XState v5 和 tween.js 版本。
- [x] `PixiBoard` 只在客户端创建 `Application`，卸载时销毁 ticker、纹理引用、监听器和 ResizeObserver。
- [x] 建立背景、棋盘、格子、路线、地标、棋子、特效和前景分层。
- [x] 使用固定逻辑坐标系和等比缩放，在横屏桌面保持完整棋盘可见。
- [x] 场景只消费快照与 `PresentationCue`，不能写入规则状态。

### 核心回合表现

- [x] 完成两颗骰子的抛出、弹跳、定格和总点数显示。
- [x] 完成路线从当前格到目标格逐段延伸。
- [x] 完成目标圈绘制与脉冲。
- [x] 路线完全消失后才开始棋子移动。
- [x] 棋子沿每个路径格跳跃，包含压缩、抛物线、阴影和落地回弹。
- [x] 折返路径必须显示到达终点后的停顿和转身。
- [x] 动画失败、失焦或超时后表现队列可以快进到权威快照。

### React UI

- [x] 完成 2 至 4 人动态 HUD、轮次、回合横幅、骰子操作和右下道具卡。
- [x] 完成座次锁定后按最终顺序逐人进行的权威起始道具三选一，以及纯外观棋子皮肤选择。
- [x] 完成道具详情底部操作层。
- [x] 完成一个地标的三张事件卡和一次纯骰子检定。
- [x] UI 通过 `GameAuthorityPort` 提交命令，动画期间锁定重复输入。
- [x] 建立 `AudioPort` 和 `NullAudioPort`，当前只发送声音 cue，不加入声音文件或播放库。

### 视觉验收

- [x] 在 `apps/web` 配置 Playwright，并提供根 `npm run e2e` 脚本。
- [x] 在 `1280 x 720`、`1600 x 900`、`1920 x 1080` 截图检查布局。
- [x] 检查 Canvas 非空、棋盘完整可见、文字不溢出、控件不重叠。
- [x] 使用固定种子捕获骰子、路线、目标圈、跳跃和事件层关键状态。
- [x] 连续重开 20 次，确认 Application、ticker 和事件监听器没有累积。

### 验收

```powershell
npm run typecheck
npm run test
npm run build
npm run e2e
```

退出条件：核心样片可以从准备阶段运行到事件结算；动画顺序严格符合规格；三个桌面尺寸无裁切和重叠。

## 10. 阶段 6：完整 65 格对局

目标：从核心样片扩展为完整的人机游戏。

当前状态：`[x]` 已完成。Web 已切换到默认 0–65 地图，九个地标、63/64/65 喧声屋胜利区域、完整表现流程、27 张事件和 12 件道具均已接入。三个模式分别通过 1,000 局无界面模拟和固定种子浏览器终局测试。详细结果见 [stage-6-full-match.md](./stage-6-full-match.md)。

### 地图与资源

- [x] 完成默认地图 66 个固定落点坐标并建立可视化校验工具。
- [x] 保留参考图的 65 格顺序、路线走向和九个地标相对位置。
- [x] 独立制作棋盘纸张、桌面、地标和特效资源，不直接使用参考截图作背景。
- [x] 大黄狗资源必须明确表现为黄色犬类。
- [x] 喧声屋视觉上统一覆盖第 63、64、65 格。
- [x] 完成至少四款纯外观棋子皮肤及 idle、active、hop、hit 动画。

### 完整流程

- [x] 接入普通移动、折返、碰撞、保护、交换位置和事件移动演出。
- [x] 接入地标横幅、事件选择、检定成功/失败和道具替换。
- [x] 接入暂停、额外行动和临时规则状态提示。
- [x] 接入胜利锁定、棋盘压暗、横向地标舞台和动态名次列表。
- [x] 结算只显示对局信息，不显示货币、经验或养成奖励。
- [x] 建立 `AudioPort` 和 `NullAudioPort`；只发送 cue，不加入声音文件或播放库。（阶段 5 已提前完成。）

### 内容目标

- [x] 至少 24 张事件，覆盖常规、检定和奇遇类别。
- [x] 普通事件格使用通用池，七个事件地标分别使用至少三张主题事件。
- [x] 至少 12 件道具，覆盖移动、干扰、检定和防御类别。
- [x] 所有内容通过 schema、资源路径和门槛可达性检查。
- [x] 默认地图可以限制不适合自己的事件或道具。

### 验收

```powershell
npm run validate:content
npm run test
npm run build
npm run e2e
```

退出条件：三个模式都可从准备阶段完成到结算；第 63、64、65 格胜利规则在画面与规则中一致；重开后所有状态和场景资源正确重置。

## 11. 阶段 7：桌面版发布质量

目标：达到可持续测试和发布的离线桌面版本。

当前状态：`[x]` 已完成。硬件加速桌面 Chromium 在 `1280 x 720` 与 `1920 x 1080` 均稳定约 60 FPS；30 分钟长跑完成 71 局并通过内存与场景生命周期检查。实现、体积和自动化证据见 [stage-7-desktop-release.md](./stage-7-desktop-release.md)。

### 稳定性与性能

- [x] 在目标设备上检查 `1280 x 720` 和 `1920 x 1080` 接近 60 FPS。
- [x] 限制设备像素比并记录首屏、完整对局资源体积。
- [x] 连续运行 30 分钟，检查内存、ticker、纹理和监听器增长。
- [x] 验证资源加载失败、页面失焦、恢复、动画超时和重开。
- [x] 页面首次加载必须有进度状态，不能出现空白 Canvas。

### 可用性

- [x] 所有主要流程可以只用鼠标完成。
- [x] 对图标按钮提供 tooltip 和可访问名称。
- [x] 检查事件卡长标题、道具说明和参与者名称溢出。
- [x] 提供动画速度和镜头运动设置。
- [x] 不实现竖屏布局；低于最低横屏尺寸时给出明确提示。
- [x] 开局使用权威单骰决定座次，同点小组单独重掷，并在界面明确展示暂定与最终顺序。
- [x] 座次锁定后，权威端为每名参与者分别抽取三件不重复起始道具候选，并按最终座次逐人完成选择。

### 自动化与发布门禁

- [x] CI 执行安装、类型检查、单元测试、内容校验、构建和关键 E2E。
- [x] E2E 覆盖 `1v1`、`1v2`、`1v3` 开局和固定种子终局。
- [x] 为规则快照和协议变更建立显式版本升级检查。
- [x] 发布构建不包含 `example_screenshots/`、录像、临时帧或调试种子面板。

### 验收

```powershell
npm ci
npm run typecheck
npm run lint
npm run test
npm run validate:content
npm run build
npm run e2e
```

退出条件：所有发布门禁通过；目标分辨率无重叠；30 分钟稳定性检查没有持续内存增长；离线模式不发送游戏网络请求。

## 12. 阶段 8：在线对局

目标：在不修改规则内核和内容定义的前提下，将 `LocalAuthority` 替换为独立 Node.js 服务中的 `RoomAuthority`。

详细方案见 [online-multiplayer-plan.md](./online-multiplayer-plan.md)。只有阶段 7 通过后才开始正式在线实现；可以更早做一次受控技术验证，但不得让实验代码进入离线关键路径。

### 前置门禁

- [x] `game-core`、`game-ai`、`game-protocol` 和 `game-content` 可以在浏览器与 Node.js 中运行。
- [x] 固定命令序列在 `LocalAuthority` 上可完全重放。
- [x] 客户端表现队列可以从任意快照重建和跳过旧动画。
- [x] 协议已经包含 schema、规则集和内容版本。

### 实施顺序

- [x] 创建 `apps/game-server` 独立 Node.js + TypeScript 常驻服务。
- [x] 先实现内存房间和两个浏览器窗口的 8 格最小地图技术样片。
- [x] 实现 2 至 4 个座位、AI 补位、房间准备、房主权限和手动开始。
- [x] 将完整 65 格地图和 PixiJS 表现队列接入在线 authority。
- [x] 技术样片由服务端独占骰子、事件随机和最终状态写入；在线 AI 已由服务端使用共享 `game-ai` 策略自动行动。
- [x] 实现重复/旧 revision 命令处理、快照同步、30 秒断线宽限、座位保留和房主超时转移。
- [x] 使用 SQLite 持久化房间、权威检查点和已处理命令结果，并支持服务进程重启恢复。
- [x] 增加监控、限流、协议指标和异常对局诊断；提供健康/Prometheus 端点、分层令牌桶、脱敏结构化日志和房间诊断上下文。
- [x] 增加 PostgreSQL 共享租约、房间所有者路由与 fencing token；同一房间只由持有有效租约的实例承载 authority，客户端可迁移到当前 owner。

### 验收

- [x] 静态 Web 客户端重新部署不影响游戏服务器中的现有房间。
- [x] 完整在线对局立即接收权威更新，并按本地表现队列延后 HUD 与行动者更新；任一客户端动画失败或掉线不阻塞其他客户端的权威回合。
- [x] 服务端直接复用经过回放测试的 `LocalAuthority`，相同种子与命令序列走同一规则实现。
- [x] 非当前行动者、伪造骰子结果、旧 revision 和协议版本不兼容命令会被拒绝。

当前状态：`[x]` 已完成。游戏服务和静态 Web 由 Playwright 独立启动，刷新客户端后仍使用标签页内恢复凭证回到原房间与座位；Chromium、Firefox、WebKit 均通过双客户端大厅、开局与刷新恢复。高延迟用例额外覆盖 250ms 单向延迟、重复游戏命令和重复权威帧，CI 关键 E2E 共 23 条并串行运行。

## 13. 账号与管理员内容平台

目标：提供独立于玩家客户端的内容管理后台，让有权限的人员通过结构化工具制作地图、事件和棋子皮肤，并通过不可变内容版本发布。

详细方案见 [admin-content-platform.md](./admin-content-platform.md)。

当前状态：`[~]` 已进入实施。独立 `apps/content-server` 已完成账号/RBAC 和内容生命周期 API；独立 `apps/admin` 已完成登录外壳、角色路由守卫、事件编辑器完整工作流，并已建立地图草稿工作台、结构化格子/地标/事件池编辑、Pixi 实时预览、核心地图校验与权威路径折返模拟。独立语义事件池、地图标记模型、奥普港显式迁移与固定种子回放测试、画布交互和正式棋盘共享渲染已完成；下一步接入游戏服务发布版本并锁定房间内容版本。

### 实施顺序

- [x] 实现最小账号登录、会话以及 `player`、`content-editor`、`admin` 角色校验。
- [x] 建立内容草稿、修订、自动校验、审核、发布、回滚和审计基础。
- [x] 实现事件编辑器，包括效果表单、事件池归属、固定种子预览和模拟；结构化效果支持按格数移动和沿路线移动到下一个不同地点。
- [x] 已建立独立 `EventPoolDefinition` 与 `MapMarkerDefinition` 并接入核心校验；`packages/content-tools/map-migration` 显式迁移旧草稿，地图编辑器读取旧修订时统一转换为新模型。
- [x] 已迁移奥普港：维修室为起点、喧声屋为终点、其余七项为地点；路径、坐标、资源、事件内容和胜利规则保持不变。
- [~] 抽卡与“移动到下一个地点”规则已优先读取语义池和 `location` 标记并通过规则测试；迁移回放覆盖 0 至 65 路径、表现坐标、胜利格、资源、固定种子事件三选一和迁移幂等性。旧字段兼容分支将在已发布内容导入后删除。
- [x] 实现地图可视化编辑器，包括路径、格子、标记、胜利区和事件池配置。工作区已重排为左侧草稿、中央主画布、右侧点选属性；格子类型选择“终点”时同步绑定胜利区并支持多个终点；Pixi 预览显示正式棋盘与真实共享贴图，并支持鉴权上传图片后点击画布放置、格子安全删除与引用重排、平滑路径、装饰/规则用途、透明度、格子/贴图拖拽、网格吸附、撤销/重做、标记增删、缩放/旋转手柄和鼠标中心滚轮缩放。
- [x] 抽取 `packages/board-renderer` 玩家端静态棋盘渲染边界；管理预览复用背景、路径、格子和地图标记渲染，编辑器只叠加命中区、路径高亮、选择框和变换手柄。
- [ ] 实现管理员皮肤制作页面，包括单图上传、质量检查、资源生成、预览和发布。
- [ ] 将当前默认内容导入为首个只读发布版本，并保证已有房间继续锁定原内容版本。

### 验收

- [x] 玩家账号不能访问管理页面或管理 API。服务端 RBAC 与管理端角色路由守卫均已覆盖。
- [~] 草稿修改不影响已发布内容和进行中的房间。管理侧草稿与发布版本已隔离，游戏服务读取发布版本尚未接入。
- [x] 地图和事件无需修改代码即可完成编辑、校验、预览和发布。事件工作流已完成；地图已接入完整内容生命周期、核心校验、正式棋盘 Pixi 预览和可视化布点交互。
- [ ] 管理员上传单张合格图片后，无需修改代码即可发布纯外观皮肤。
- [x] 发布与回滚均生成不可删除的审计记录。

## 14. 每项任务的完成定义

任务只有同时满足以下条件才能标记为 `[x]`：

- 实现符合 `game-design.md`、`technical-design.md` 和 `visual-design.md`。
- 新增或修改的规则拥有对应测试，不能只依靠手工点击。
- 类型检查、相关测试和构建通过。
- 不引入角色属性、角色技能、养成或皮肤规则加成。
- 不把默认地图的 65 格写成共享规则常量。
- 不在 React/PixiJS 组件中直接修改权威规则状态。
- 新依赖有明确用途并锁定版本，没有无理由引入大型框架。
- 文档、命令和实际目录保持一致。
- 工作区不包含参考截图、录像、临时帧、构建产物或本地密钥。

## 15. 暂缓与明确不做

首个离线桌面版本暂缓：

- 竖屏、手机和平板布局。
- 管理员地图、事件和皮肤编辑平台按阶段 13 独立实施；普通玩家自制内容不在当前范围。
- 账号、好友、公开匹配、排行榜和观战。
- 实际音效、背景音乐和音频播放库。

永久不做：

- 角色属性和属性检定修正。
- 角色技能、等级、经验、装备或养成。
- 棋子皮肤的数值、技能、稀有度或成长加成。

玩法扩展通过版本化地图、格子数量、事件、道具、参与人数和规则集实现，不通过角色系统实现。
